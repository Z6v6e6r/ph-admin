export interface TelegramStationMapping {
  key: string;
  stationId: string;
  stationName: string;
  groupChatId: string;
}

export interface TelegramClientStationState {
  stationId: string;
  stationName: string;
  groupChatId: string;
}
