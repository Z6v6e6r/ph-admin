# Include in every public reserve server before enabling source export.
# A public client must never borrow the reserve's source-IP permission.
location = /__phab_traffic_policy {
    return 404;
    access_log off;
}
